# Same settings as base; only the weights change.
# "models/..." resolves against $MODELS_DIR (default ./models), see scripts/serve.py.
model: models/w8
dtype: float16
max_model_len: 8192
gpu_memory_utilization: 0.85
max_num_seqs: 64
seed: 0
enable_prefix_caching: false
