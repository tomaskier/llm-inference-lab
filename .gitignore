# Python
/.venv
__pycache__/
*.pyc

# Model weights (GBs, rebuilt with scripts/quantize.py or pulled from Kaggle)
/models

# Secrets (tunnel URL, API key)
.env
!.env.example

# Jupyter
.ipynb_checkpoints/

# Result archives downloaded from Kaggle (unzip into results/, which is tracked)
*.zip

# Personal notes
notas
# Kaggle venvs (quant, bench, eval)
/.venvs

# Local Prometheus target (tunnel URL)
observability/targets/*.json

# Server logs and scratch output
*.log
*.partial.json
/build
